export interface RepentogonEntityWeapon {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonGridEntityDecoration {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  tempValue: boolean;
}

export interface RepentogonGridEntityFire {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonGridEntityGravity {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonGridEntityLock {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonGridEntityStaircase {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonGridEntityStatue {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonGridEntityTeleporter {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonGridEntityTrapdoor {
  temporaryValue: boolean;
}

export interface RepentogonGridEntityWeb {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonLevelGenerator {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}

export interface RepentogonLevelGeneratorRoom {
  temporaryValue: boolean;
}

export interface RepentogonMultiShotParams {
  // Will fill this whole interface out later. For now this is here so
  // `AddCallbackParametersRepentogon` can be finished.
  temporaryValue: boolean;
}
