/**
 * This class is for REPENTOGON, an exe-hack which expands the modding API.
 *
 * @noSelf
 * @see https://repentogon.com/
 */
declare namespace DebugRenderer {
  /**
   * Returns the `Shape` from the provided index.
   *
   * @param index
   * @param unknown What this argument does is currently unknown.
   */
  function Get(index: int, unknown: boolean): Shape;
}
