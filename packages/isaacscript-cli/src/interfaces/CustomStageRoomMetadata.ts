/**
 * Metadata about a custom stage room. Each custom stage object contains an array of these objects.
 */
export interface CustomStageRoomMetadata {
  roomVariant: number;
  roomShape: number;
  weight: number;
}
