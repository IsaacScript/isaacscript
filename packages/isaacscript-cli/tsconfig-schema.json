{
  "title": "tsconfig.json with IsaacScript",
  "description": "JSON schema for the TypeScript compiler's configuration file with IsaacScript",
  "$schema": "http://json-schema.org/draft-07/schema",
  "allOf": [
    {
      "$ref": "https://raw.githubusercontent.com/TypeScriptToLua/vscode-typescript-to-lua/master/tsconfig-schema.json"
    }
  ],
  "properties": {
    "isaacscript": {
      "description": "IsaacScript compiler options.",
      "type": "object",
      "definitions": {
        "//": {
          "reference": "https://isaacscript.github.io/main/custom-stages/"
        }
      },
      "properties": {
        "customStages": {
          "description": "List of custom stage objects.",
          "type": "array",
          "items": {
            "description": "Describes TypeScriptToLua plugin",
            "type": "object",
            "required": ["name"],
            "properties": {
              "xmlPath": {
                "description": "Path to the XML file that contains the rooms for the custom stage (created with Basement Renovator)",
                "type": "string"
              },
              "roomVariantPrefix": {
                "description": "An arbitrarily chosen prefix in the range of 1001-9999",
                "type": "number"
              }
            }
          }
        }
      }
    }
  },
  "allowTrailingCommas": true
}
