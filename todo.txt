- july 20th is trip

- custom stage level streak text - need to render on top of HUD
- custom trapdoors
- custom bosses for vanilla stages

1) foo += 1 auto-fixed to foo++. (prefer-plusplus)
2) foo -= 1 auto-fixed to foo--. (prefer-plusplus)
3) ++foo auto-fixed to foo++. (prefer-postfix-plusplus)
4) --foo auto-fixed to foo--. (prefer-postfix-plusplus)
5) foo++ combined with any operator throws warning. (no-plusplus-operators)
6) foo-- combined with any operator throws warning. (no-plusplus-operators)

{
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": ":not(UnaryExpression[operator='void'],ExpressionStatement,SequenceExpression,ForStatement[update.type=UpdateExpression]) > UpdateExpression[prefix='false'], ForStatement[init.type=UpdateExpression] > UpdateExpression[prefix='false'], ForStatement[test.type=UpdateExpression] > UpdateExpression[prefix='false']",
        "message": "BANNED"
      },
      {
        "selector": "SequenceExpression > UpdateExpression[prefix='false']:last-child",
        "message": "BANNED"
      }
    ]
  }
}
