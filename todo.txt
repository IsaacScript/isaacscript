- add git pull at the start of publish

- lint rule for unknown sets

- github action - release notes equal to commit message
- github action - use string after "release" for change log

- add variables to uploader github action
- make isaac-mod-config-menu use github action

- racing-plus + babies mod + config menu CI ACTIONS
- convert-xml-to-json - enable check

- try new TypeDoc branch
- typedoc bug - spawnCustomGridEntity documentation doesn't have question marks on final arguments
- investigate EXECUTE_CMD crashing

- lint rule audit:
  - [...require("eslint/use-at-your-own-risk").builtinRules.keys()]
  - Object.keys(require("@typescript-eslint/eslint-plugin").configs.all.rules)

----------------------------------------------------------------------------------------------------

- when typescript 5.0 comes out, remove "newLine" in tsconfig
- when typescript 5.0 comes out, remove all tsconfig duplication with `extends` (e.g. tsconfig.node.json)

- get docs working with ModCallbackCustom
  - waiting on issue: https://github.com/algolia/docsearch/issues/1658
  - https://support.algolia.com/hc/en-us/requests/538679

- use stack overflow to remove isaac-typescript-definitions from deps
  - difficult, do it when I have the courage

- move to pnpm: https://github.com/pnpm/pnpm/issues/6030
