{
  "@schema": "https://raw.githubusercontent.com/complete-ts/complete/refs/heads/main/packages/complete-cli/schemas/package-metadata-schema.json",
  "dependencies": {
    "figlet": {
      "lock-version": true,
      "lock-reason": "https://github.com/patorjk/figlet.js/issues/139"
    },
    "typescript": {
      "lock-version": true,
      "lock-reason": "https://github.com/TypeScriptToLua/TypeScriptToLua/blob/master/package.json#L45"
    }
  },
  "devDependencies": {
    "eslint": {
      "lock-version": true,
      "lock-reason": "ESLint 10 does not seem to work yet.",
      "description": "Even though ESLint is part of complete-lint, it is also part of other dependencies, so npm will take the more conservative approach and choose to install the older version. Thus, we must explicitly specify it as a root dependency to force the latest version."
    }
  }
}
