export interface Config {
  projectName: string; // The name of your project directory
  modsDirectory: string; // The "mods" directory that lives next to the "isaac-ng.exe" program
  saveSlot: number; // The save slot that you test your mod on
}
